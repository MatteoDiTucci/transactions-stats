package application;

import javax.inject.Singleton;
import java.math.BigDecimal;
import java.time.Instant;

@Singleton
public class LogTransaction {

    public void foo(BigDecimal amount, Instant timestamp) {

    }
}
